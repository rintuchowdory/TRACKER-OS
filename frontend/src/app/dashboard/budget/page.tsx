import { PlaceholderSection } from "@/components/PlaceholderSection";
import { SECTIONS } from "@/lib/sections";

export default function BudgetPage() {
  return <PlaceholderSection section={SECTIONS.budget} />;
}
